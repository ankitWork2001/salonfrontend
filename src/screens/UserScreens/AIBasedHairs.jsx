import { View, Text, StyleSheet } from 'react-native'

export default function AIBasedHairs() {
  return (
    <View style={styles.container}>
      <Text>AIBasedHairs</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});